npm -- is a global command comes with node
npm --version 

local dependency - use it only in this particular project
npm i <package name >

global dependency - use it in any project
npm install -g <packagename>  //to install any package globally
sudo npm instal -g <package name> //for mac


what is pakage,json and how is it made?

pakage.json - manifest file (stores important info about project/package)
manual approach (create package.json in the root, create properties etc)
npm init (step by step press enter to skip)
npm init -y (everything default)